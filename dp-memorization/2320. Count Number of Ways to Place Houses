# Time complexity: O(n)
# Space complextiy: O(1)

# One side is Fibonacci-type because every valid arrangement of length n can be built from arrangements of length n−1 and n−2.
class Solution:
    def countHousePlacements(self, n: int) -> int:
        # dp[0] = 1; dp[1] = 2 ( empty or one house)
        MOD = 10**9 + 7
        prev2, prev1 = 1, 2
        for _ in range(2, n+1):
            prev1, prev2 = (prev1+prev2) % MOD, prev1
        return (prev1 * prev1) % MOD

