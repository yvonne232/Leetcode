# Method 1: time complexity O(n); space complexity O(n). I think this is better
class Solution:
    def countGoodStrings(self, low: int, high: int, zero: int, one: int) -> int:
        MOD = 10 ** 9 + 7
        dp = [0] * (high + 1)
        dp[0] = 1 # one way to build a string of 0

        for i in range(1, high+1):
            if i >= zero:
                dp[i] = (dp[i] + dp[i-zero]) % MOD
            if i >= one:
                dp[i] = (dp[i] + dp[i-one]) % MOD
        
        return sum(dp[i] for i in range(low, high+1)) % MOD

# Method 2: time complexity O(n); space complexity O(n).
class Solution:
    def countGoodStrings(self, low: int, high: int, zero: int, one: int) -> int:
        MOD = 10 ** 9 + 7
        
        @cache
        def dfs(length):
            if length > high:
                return 0
            count = 1 if length >= low else 0
            count = (count + dfs(length + zero)) % MOD
            count = (count + dfs(length + one)) % MOD
            return count

        return dfs(0)

Example:

low=3, high=3, zero=1, one=1 → dp[3]=8
dp[1] += dp[1 - zero] = dp[0] = 1
dp[1] += dp[1 - one]  = dp[0] = 1
Then dp[1] = 2
dp[2] += dp[2 - zero] = dp[1] = 2
dp[2] += dp[2 - one]  = dp[1] = 2
Then dp[2] = 4
dp[3] += dp[3 - zero] = dp[2] = 4
dp[3] += dp[3 - one]  = dp[2] = 4
Then dp[3] =8
