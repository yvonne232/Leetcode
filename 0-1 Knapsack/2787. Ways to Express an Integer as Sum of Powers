Time complexity: O(m*n) 
for p in powers:          # m times
    for s in range(n, p-1, -1):   # up to n times
Space complexity: O(n)

class Solution:
    def numberOfWays(self, n: int, x: int) -> int:
        MOD= 10**9 + 7

        # build i ** x
        powers = []
        i = 1
        while True:
            p = i ** x
            if p > n:
                break
            powers.append(p)
            i += 1
        
        dp = [0] * (n+1)
        dp[0] = 1

        for p in powers:
            for s in range(n, p-1, -1):
                dp[s] = (dp[s-p] + dp[s]) % MOD

        return dp[n]

dp[s] = number of ways to form sum s using the powers processed so far
Case 1: Do NOT use p dp[s], Then the number of ways to make sum s stays the same:
Case 2: Use p; The number of ways to form s - p is dp[s-p]
