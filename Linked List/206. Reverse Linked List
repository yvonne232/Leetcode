class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        prev = None
        curr = head

        # the loop stops when curr is none
        while curr:
            nxt_temp = curr.next
            curr.next = prev
            prev = curr
            curr = nxt_temp

        return prev
